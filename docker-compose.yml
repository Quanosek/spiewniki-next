services:
  spiewniki:
    container_name: spiewniki
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3006:3000'
    restart: always
    environment:
      - NODE_ENV=production
